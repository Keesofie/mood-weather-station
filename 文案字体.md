:root {
  /* --------------------------------------
     1. 正文字体（系统原生栈 · 零加载成本）
     适用于：所有UI文字、用户短句、卡片描述
     -------------------------------------- */
  --font-body: -apple-system, BlinkMacSystemFont, 
               "SF Pro Text", "SF Pro Display",
               "PingFang SC", "Helvetica Neue", 
               "Microsoft YaHei", "微软雅黑",
               "Noto Sans SC", sans-serif;

  /* --------------------------------------
     2. 标题/品牌字体（衬线体 · 文艺感）
     优先加载思源宋体（Google Fonts），
     加载失败则降级至各平台原生衬线体
     适用于：大字号情绪天气、日签语录、品牌Logo
     -------------------------------------- */
  --font-heading: "Noto Serif SC", "Source Han Serif SC",
                  "思源宋体", "Songti SC", "华文宋体",
                  "SimSun", "宋体", Georgia, serif;

  /* --------------------------------------
     3. 数字/数据字体（等宽数字优先）
     适用于：日期时间、压力波形图数值、统计计数
     -------------------------------------- */
  --font-number: "SF Pro Display", "SF Pro Text",
                 "Roboto", "Helvetica Neue",
                 "PingFang SC", "Microsoft YaHei", 
                 sans-serif;
  font-variant-numeric: tabular-nums;

  /* --------------------------------------
     4. 互动/按钮字体（圆润亲和 · 可选）
     适用于：底部主按钮、漂流瓶入口、标签
     注：此字体需加载，若追求极致稳妥可删除此行，
     回退至 --font-body
     -------------------------------------- */
  --font-action: "ZCOOL QingKe HuangYou", "站酷庆科黄油体",
                 "PingFang SC", "Microsoft YaHei", 
                 sans-serif;
}









/* ========== 应用到具体类名 ========== */

/* 1. 品牌标题（首页大天气 + 日签） */
.brand-title,
.weather-display,
.daily-quote {
  font-family: var(--font-heading);
  font-weight: 700;
  letter-spacing: 0.04em; /* 增加呼吸感 */
}

/* 2. 所有正文、卡片描述、用户短句 */
.body-text,
.card-description,
.user-sentence,
.location-tag {
  font-family: var(--font-body);
  font-weight: 400;
  line-height: 1.6; /* 提升阅读舒适度 */
}

/* 3. 数据数字（日期、波形数值） */
.data-number,
.card-date,
.wave-value {
  font-family: var(--font-number);
  font-variant-numeric: tabular-nums;
  font-weight: 500;
}

/* 4. 主按钮与互动元素 */
.primary-button,
.action-button,
.drift-bottle-btn {
  font-family: var(--font-action);
  font-weight: 400;
}

/* 5. 备用降级方案：若 --font-action 未加载，强制回退系统字体 */
.primary-button {
  font-family: var(--font-action), var(--font-body);
}