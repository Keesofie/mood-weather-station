请基于 HTML/CSS/JavaScript 开发一个“情绪卡片左右翻页”交互页面。严格遵守以下规则：

1. 核心翻页引擎（必须实现）

使用 “拖拽式轮播（Drag-to-Snap）” ，不是点击式轮播。

监听 mousedown/mousemove/mouseup（电脑）和 touchstart/touchmove/touchend（手机）。

翻页阈值：拖拽距离超过当前卡片宽度 30% 即触发翻页，否则回弹至原位。

拖拽过程中，当前卡片执行 transform: translateX({偏移量}) rotate({偏移量*0.08}deg) scale(0.95 + {偏移量/10000})，营造物理拉扯感。

2. 响应式差异化（关键）

Mobile（<768px）：卡片宽度 90vw，左右滑动无惯性，松手即停；底部圆点指示器使用小圆点（直径8px）；隐藏左右箭头按钮。

Desktop（>=768px）：卡片宽度固定 600px，启用“惯性滑动”（快速甩动能滑多张）；显示左右半透明箭头按钮（< >），且支持键盘 ArrowLeft / ArrowRight 按键翻页。

3. 翻页过渡动画

翻页时，当前卡片向左侧移出（若翻下一页），透明度 1→0；新卡片从右侧移入（透明度 0→1），伴随微缩放（0.92→1.0）。

使用 CSS will-change: transform 开启硬件加速。

背景色渐变切换需滞后卡片切换 150ms，形成“天色先变”的层次感。

所有卡片切换动画时长统一为 350ms，缓动函数采用 cubic-bezier(0.23, 1, 0.32, 1)。

4. 堆叠预览效果（新增核心视觉）

在渲染当前卡片（索引 i）时，必须同时渲染其左邻卡片（索引 i-1）和右邻卡片（索引 i+1），形成扑克牌堆叠感。

左邻卡片：位于当前卡片左侧，应用 transform: scale(0.85) translateX(-55%)，透明度 0.45，z-index 低于当前卡片。

右邻卡片：位于当前卡片右侧，应用 transform: scale(0.85) translateX(55%)，透明度 0.45，z-index 低于当前卡片。

动态联动：在用户拖拽过程中，必须根据拖拽偏移量（dragOffset）实时动态更新相邻卡片的缩放值（范围 0.85 至 1.0）和透明度（范围 0.45 至 1.0）。例如，当手指向右拖动（准备翻上一张）时，左侧卡片应逐渐放大并变亮，模拟“被抽出来”的物理纵深。

卡片之间的层级关系严格遵循：当前卡片 > 即将翻到的卡片 > 被翻走的卡片。

5. 指示器与页码

底部居中显示圆点（Dots），当前页高亮且放大（1.3倍）。

左上角固定显示 “第 X / 共 Y 页” 文字。

6. 性能与触控优化

若检测到 pointer: coarse（触屏设备），自动忽略所有 :hover 样式，并将堆叠预览的透明度略微降低（至 0.35），避免视觉杂乱。

使用 requestAnimationFrame 驱动拖拽动画，避免掉帧；禁止在拖拽过程中触发任何 DOM 重排操作（如修改 innerHTML）。






